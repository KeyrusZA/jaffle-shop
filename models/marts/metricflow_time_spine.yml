models:
  - name: metricflow_time_spine
    description: Daily time spine used by the Semantic Layer for time-based metric calculations.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: Daily date used as the standard Semantic Layer time spine grain.
        granularity: day
